# TODO: here add only compilation logic like compile protocol from protocol discusiion
# Drop all other rows except covariates, treatment, outcome, effect modifiers
# This would add new dataset id and summary so generate.
# Then make a trasnformation plan 
# Ask user to accept final dataset and trasnformation plan and this new things first would be saved in local state.

# if user accept everything then you can set it by ochestration_state.set("node name ") and mapping with respective data.
# and if ochestration data is changed like id is changed while node state is not confimed yet you have to rerun the stuff.. 


