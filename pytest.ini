[pytest]
# Look for tests under src/tests
testpaths = src/tests
python_files = test_*.py *_test.py
addopts = -ra -p no:rerunfailures

# Register custom markers
markers =
    integration: tests that hit real external services (Gemini API, etc.)
    deepeval: opt-in LLM evaluation tests that use DeepEval and a real judge model.

# Turn warnings into errors in tests (comment out if too strict)
filterwarnings =
    error
    ignore:scipy\.optimize: DeprecationWarning
    ignore:.*L-BFGS-B solver are deprecated.*:DeprecationWarning

log_cli = true
log_cli_level = INFO
